48|Attempt to create a container with "role: FleetAgent" but override the volume mount to request 'ReadWrite' access. Verify that the Pkl-evaluator rejects the configuration before the VM boot process starts.
49|Create a container with "role: Anchor". Verify that the orchestrator allows 'ReadWrite' access to the shared volume, confirming the role-based elevation is functioning.
50|Execute the preflight check for the VirtioFS mount path "/Users/kieranlal/.containers/Volumes/test/". Force a failure by simulating a missing TCC Full Disk Access grant. Capture the specific "PreflightError" reported by the Orchestrator.
51|Submit a legacy Pkl config (Plan 104 style, missing 'role' and 'accessMode' fields) to the orchestrator. Verify that the CLM realizes it as a "FleetAgent" with "ReadOnly" access, ensuring no regression for existing manifests.
52|Create a malformed HonchoPod.pkl file where a SharedVolumeMount is missing the mandatory 'container_path'. Verify that the Pkl evaluation engine throws a validation error before the ContainerLifecycleManager is even invoked.
53|Create a 'CreativePod' named "video-render-node" with gpuPolicy set to "MetalParavirtualized". Inspect the VM configuration to verify the VZVirtioGraphicsDevice is attached. Attempt to execute a command that queries the GPU device driver to confirm hardware visibility.
54|Create two CreativePods ("renderer-a", "renderer-b"). Map a shared volume with 'DirectMMap' signaling. Have renderer-a write an asset descriptor to the VirtioFS shared volume and have renderer-b read it to initiate a render sequence.
55|Simulate a creative asset update by having the Anchor perform 100 atomic renames of "routing.json" in 1 second. Verify that the FleetAgents (renderer-a/b) detect the changes via kqueue without CPU thrashing (verify CPU remains < 5%).
56|Manually inject a "Red" state violation into the LTL Runtime Monitor (e.g., attempt to Shutdown an Active container without completing a clean stop). Trigger the OptimizerAgent to: 1. Analyze the trace data. 2. Propose a "Mutation Recipe" to return the state machine to G
57|Create a failing container configuration (Test 196 Red). Retrieve the Recipe Applicability Chain from the OptimizerAgent. Verify that the chain correctly identifies the dependency path (A -> B -> C) required to reach a Green state.
58|Attempt to update a running service's 'signaling' policy from 'KQueue' to 'None' while the pod is Active. Verify that the Harness rejects the change as a violation of the "Live-Constraint" invariant defined in the Pkl schema.
59|Create an AgentSpec "Immutable-Assistant". Attempt to update its 'system_prompt' using an in-place mutation tool. Verify that the system denies the mutation and forces the creation of a "v2" AgentSpec, ensuring auditability of the agent lineage.
60|1. Create a container with 'checkpointing: true'. 2. Run a workflow for 10 steps. 3. Trigger a harness checkpoint (serialize harness + pod state). 4. Simulate a harness crash (kill the orchestrator). 5. Restart the orchestrator and restore har
61|Create 50 agents using VSOCK for signaling instead of kqueue. Check the memory footprint of the orchestrator. Verify that the system supports this density without file descriptor exhaustion.
62|Exec into a FleetAgent and attempt to write a file to the Anchor's local filesystem path (outside of the VirtioFS mount). Verify that the Seatbelt/Sandbox policy blocks the system call and logs a security violation.
63|Create a container with a Pkl config that is valid but exceeds the currently active "Dynamic Context" (e.g., attempt to request 4 vCPUs on a node that only has 2 vCPUs unallocated). Verify the orchestrator rejects the call at the "Context Routing" stage, despite the Pkl sc
64|Create a 'CreativePod' with a requirement for "MetalParavirtualized" graphics. Execute this on a host environment where the GPU is disabled or missing. Verify that the Preflight/Capability check identifies the hardware absence and returns a "HostCapabilityError".
65|Provide the agent with the last 60 seconds of trace data from a 'renderer-a' pod showing p99 latency spikes. Ask the agent to: "Analyze this trace and identify if the bottleneck is in VirtioFS throug
66|Simulate a "Formal Execution" error where the VM fails to boot due to an 'Out of Memory' condition (despite passing static Pkl checks). Feed this error result back to the agent and ask it to: "Propose a mutation to the Pkl sche
67|Attempt to force a container into the "Active" state by bypassing the "Preflight" stage. Verify that the LTL Runtime Monitor detects the invalid transition and immediately reverts the state to "Red" (StateDrift).
68|Create a container that hangs in the "Initializing" state indefinitely. Verify that the LTL monitor triggers a "Liveness Violation" after the 30-second timeout and invokes the OptimizerAgent to attempt a recovery restart.
